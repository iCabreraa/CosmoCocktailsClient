import CartClient from "@/components/cart/CartClient";

export const metadata = {
  title: "Cart | CosmoCocktails",
  description: "Review your selected cocktails",
};

export default function CartPage() {
  return <CartClient />;
}
