import CheckoutClient from "@/components/checkout/CheckoutClient";

export const metadata = {
  title: "Checkout | CosmoCocktails",
  description: "Finalize your order",
};

export default function CheckoutPage() {
  return <CheckoutClient />;
}
